const RetrySettingsUiSchema = {
  retryInterval: {
    "ui:widget": "select",
  },
  maxRetries: {
    "ui:widget": "select",
  },
  timeout: {
    "ui:widget": "select",
  },
};

export default RetrySettingsUiSchema;
